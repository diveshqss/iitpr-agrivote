
// MongoDB commands to hash passwords as "password" for all users
// Run these commands in mongosh or MongoDB Compass

// Connect to AgriVote database
use AgriVote;

// Simple update - sets hashed_password to plain "password" (demo only)
// DO NOT USE IN PRODUCTION - use proper bcrypt hashing
db.users.updateMany(
  { hashed_password: { $exists: false } }, // Only update users without hashed_password
  { $set: { hashed_password: "password" } }
);

// To check updated users
db.users.find({}, { email: 1, hashed_password: 1 }).toArray();

